name: 'Hello World Custom Action'
description: 'Hello World Custom Action'

inputs:
  who-to-greet:
    description: 'Who to Greet'
    required: true
    default: 'World'

outputs:
  time:
    description: 'The time we greet you'

runs:
  using: 'node16'
  main: index.js